# Example .env file for sync.sh

# Comma-separated list of source directories to sync
SOURCE_DIRS="/path/to/source1,/path/to/source2"

# Destination directory for the sync
DEST_DIR="/path/to/destination"

# Comma-separated list of directories or files to exclude from sync
EXCLUDES_DIRS=".git/,node_modules/,some/other/dir/,file_to_exclude.txt"